{
  "steps": [
    {
      "type": "read_files",
      "config": {
        "path": "{{ output_root }}/{{ outline.title | snakecase | upcase }}.md",
        "content_key": "document"
      }
    }
  ]
}
