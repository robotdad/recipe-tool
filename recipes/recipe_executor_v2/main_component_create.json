{
  "steps": [
    {
      "type": "execute_recipe",
      "recipe_path": "recipes/recipe_executor_v2/component_template.json",
      "context_overrides": {
        "component_id": "main",
        "component_name": "Main component",
        "component_description": "The CLI entry point and main module",
        "existing_code_text": "{{existing_code_text}}",
        "existing_code_section": "{{existing_code_section}}"
      }
    }
  ]
}
