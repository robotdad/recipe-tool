{
  "steps": [
    {
      "type": "execute_recipe",
      "recipe_path": "recipes/recipe_executor_v2/component_template.json",
      "context_overrides": {
        "component_id": "steps/base",
        "component_name": "Steps Base component",
        "component_description": "The base classes and registry for steps",
        "existing_code_text": "{{existing_code_text}}",
        "existing_code_section": "{{existing_code_section}}"
      }
    }
  ]
}
