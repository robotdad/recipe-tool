{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "debugpy",
      "request": "launch",
      "name": "recipe-executor",
      "cwd": "${workspaceFolder}",
      "module": "recipe_executor.main",
      "consoleTitle": "recipe-executor"
      // "justMyCode": false // Set to false to debug external libraries
    }
  ]
}
